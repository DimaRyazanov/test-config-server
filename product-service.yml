spring:
  data:
    mongodb:
      auto-index-creation: true
      authentication-database: admin
      database: product-service
      port: 27017
      host: localhost
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
test:
  name: TestName1